services:
  landing:
    container_name: landing
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    networks:
      - web
networks:
  web:
    external: true
    name: trainers-manager_training_net